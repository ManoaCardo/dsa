const { Client, EmbedBuilder, PermissionsBitField } = require("discord.js");
module.exports = {
    name:"buy",
    description: 'buy',
    type:1,
    options: [
 ],
  run: async(client, interaction) => {

    interaction.reply({content: "Link: https://pornhub.com/"})
}

};